# Current State

## Repository purpose

`NexusRealtime-Ideas` is the folder-driven domain catalogue and scheduled-worker memory repo for NexusRealtime idea discovery.

## Current status

- Repo bootstrapped.
- Scope cascade exists.
- Scheduled-task architecture is planned but no scheduled tasks should be created yet.
- The first useful work is catalogue-only dry-run work through saved-state lanes.

## Current constraint

This repo must not become a replacement for ProtoKits.

ProtoKits owns reusable kit implementation.

Ideas owns domain thinking, catalogue records, candidate packets, and builder queues.

## Next safest action

Run one manual catalogue dry run for `saved-state-01.md` before creating any scheduled tasks.
