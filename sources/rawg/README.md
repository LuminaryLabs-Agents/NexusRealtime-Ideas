# RAWG

RAWG is the current large-game source candidate for the import pipeline.

## Notes

- The official API requires an API key.
- The CLI supports `import-rawg` for paged ingest.
- Imported records should land in `games/` and `publish-games/`.
