# sources

This folder tracks the intake sources used to populate game profiles.

## Current source path

- `RAWG API` via `node scripts/game-profile-cli.mjs import-rawg`

## Intended use

- Record source lists and import metadata.
- Keep the source of each profile traceable.
- Support large ingest runs that can reach 200,000+ existing games.
