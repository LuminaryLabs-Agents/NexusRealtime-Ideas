# Idea Intake

Place each raw idea in its own folder:

```text
ideas/intake/<id-slug>/
  idea.md
  idea.json
  status.md
```

The harness processes intake folders independently and also generates one fresh seed idea per run.
